spring.application.name=studbuds

server.tomcat.accesslog.enabled=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.datasource.url=${STUDBUDS_DATABASE_URL}
spring.datasource.username=${STUDBUDS_DATABASE_USER}
spring.datasource.password=${STUDBUDS_DATABASE_PASSWORD}
